server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Health probe (Cloud Run/K8s).
    location = /healthz {
        access_log off;
        return 200 "ok\n";
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}
